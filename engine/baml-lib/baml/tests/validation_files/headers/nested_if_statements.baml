# Main Function
fn ComplexHeaderTest() -> string {
    # Initialization
    let items = ["a", "b", "c"];
    let result = "";
    
    ## Processing Loop
    let e = if true {

        # Markdown Header tester
        let hello = "Hello";

        ### Item Analysis
        let processed = 2;
        
        ### Validation
        let z = if true {

            # Markdown Header tester in statement
            let hello = "Hello";

            ### another header in the statement
            let hello = "Hello";

            ### True totally
            12
        } else {
            ### False other branch
            13
        };

        ## very useful header
        let hello = "Hello";

        ### Result
        result

    } else {
        ### False other branch
        13
    };
    
    # Final Processing
    let final_result = result.reverse();
    
    # Return Result
    final_result
} 
