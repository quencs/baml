// ----------------------------------------------------------------------------
//
//  Welcome to Baml! To use this generated code, please run the following:
//
//  $ cargo add baml-client
//
// ----------------------------------------------------------------------------

// This file was generated by BAML: please do not edit it. Instead, edit the
// BAML files and re-generate this code using: baml-cli generate
// You can install baml-cli with:
//  $ cargo install baml-cli

use serde::{Deserialize, Serialize};
use std::collections::HashMap;

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct BooleanLiterals {
    pub alwaysTrue: String,

    pub alwaysFalse: String,

    pub eitherBool: String,
}

impl BooleanLiterals {
    /// Create a new BooleanLiterals instance
    pub fn new(alwaysTrue: String, alwaysFalse: String, eitherBool: String) -> Self {
        Self {
            alwaysTrue,
            alwaysFalse,
            eitherBool,
        }
    }
}

impl Default for BooleanLiterals {
    fn default() -> Self {
        Self::new(String::new(), String::new(), String::new())
    }
}

// BAML trait implementations
impl baml_client_rust::types::ToBamlValue for BooleanLiterals {
    fn to_baml_value(self) -> baml_client_rust::BamlResult<baml_client_rust::types::BamlValue> {
        let mut map = baml_client_rust::types::BamlMap::new();
        map.insert("alwaysTrue".to_string(), self.alwaysTrue.to_baml_value()?);
        map.insert("alwaysFalse".to_string(), self.alwaysFalse.to_baml_value()?);
        map.insert("eitherBool".to_string(), self.eitherBool.to_baml_value()?);
        Ok(baml_client_rust::types::BamlValue::Class(
            "BooleanLiterals".to_string(),
            map,
        ))
    }
}

impl baml_client_rust::types::FromBamlValue for BooleanLiterals {
    fn from_baml_value(
        value: baml_client_rust::types::BamlValue,
    ) -> baml_client_rust::BamlResult<Self> {
        match value {
            baml_client_rust::types::BamlValue::Class(_class_name, map) => {
                let alwaysTrue = map
                    .get("alwaysTrue")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'alwaysTrue' in BooleanLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                let alwaysFalse = map
                    .get("alwaysFalse")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'alwaysFalse' in BooleanLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                let eitherBool = map
                    .get("eitherBool")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'eitherBool' in BooleanLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                Ok(Self::new(alwaysTrue, alwaysFalse, eitherBool))
            }
            _ => Err(baml_client_rust::BamlError::deserialization(format!(
                "Expected class, got {:?}",
                value
            ))),
        }
    }
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ComplexLiterals {
    pub state: String,

    pub retryCount: String,

    pub response: String,

    pub flags: String,

    pub codes: String,
}

impl ComplexLiterals {
    /// Create a new ComplexLiterals instance
    pub fn new(
        state: String,
        retryCount: String,
        response: String,
        flags: String,
        codes: String,
    ) -> Self {
        Self {
            state,
            retryCount,
            response,
            flags,
            codes,
        }
    }
}

impl Default for ComplexLiterals {
    fn default() -> Self {
        Self::new(
            String::new(),
            String::new(),
            String::new(),
            String::new(),
            String::new(),
        )
    }
}

// BAML trait implementations
impl baml_client_rust::types::ToBamlValue for ComplexLiterals {
    fn to_baml_value(self) -> baml_client_rust::BamlResult<baml_client_rust::types::BamlValue> {
        let mut map = baml_client_rust::types::BamlMap::new();
        map.insert("state".to_string(), self.state.to_baml_value()?);
        map.insert("retryCount".to_string(), self.retryCount.to_baml_value()?);
        map.insert("response".to_string(), self.response.to_baml_value()?);
        map.insert("flags".to_string(), self.flags.to_baml_value()?);
        map.insert("codes".to_string(), self.codes.to_baml_value()?);
        Ok(baml_client_rust::types::BamlValue::Class(
            "ComplexLiterals".to_string(),
            map,
        ))
    }
}

impl baml_client_rust::types::FromBamlValue for ComplexLiterals {
    fn from_baml_value(
        value: baml_client_rust::types::BamlValue,
    ) -> baml_client_rust::BamlResult<Self> {
        match value {
            baml_client_rust::types::BamlValue::Class(_class_name, map) => {
                let state = map
                    .get("state")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'state' in ComplexLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                let retryCount = map
                    .get("retryCount")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'retryCount' in ComplexLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                let response = map
                    .get("response")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'response' in ComplexLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                let flags = map
                    .get("flags")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'flags' in ComplexLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                let codes = map
                    .get("codes")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'codes' in ComplexLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                Ok(Self::new(state, retryCount, response, flags, codes))
            }
            _ => Err(baml_client_rust::BamlError::deserialization(format!(
                "Expected class, got {:?}",
                value
            ))),
        }
    }
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct IntegerLiterals {
    pub priority: String,

    pub httpStatus: String,

    pub maxRetries: String,
}

impl IntegerLiterals {
    /// Create a new IntegerLiterals instance
    pub fn new(priority: String, httpStatus: String, maxRetries: String) -> Self {
        Self {
            priority,
            httpStatus,
            maxRetries,
        }
    }
}

impl Default for IntegerLiterals {
    fn default() -> Self {
        Self::new(String::new(), String::new(), String::new())
    }
}

// BAML trait implementations
impl baml_client_rust::types::ToBamlValue for IntegerLiterals {
    fn to_baml_value(self) -> baml_client_rust::BamlResult<baml_client_rust::types::BamlValue> {
        let mut map = baml_client_rust::types::BamlMap::new();
        map.insert("priority".to_string(), self.priority.to_baml_value()?);
        map.insert("httpStatus".to_string(), self.httpStatus.to_baml_value()?);
        map.insert("maxRetries".to_string(), self.maxRetries.to_baml_value()?);
        Ok(baml_client_rust::types::BamlValue::Class(
            "IntegerLiterals".to_string(),
            map,
        ))
    }
}

impl baml_client_rust::types::FromBamlValue for IntegerLiterals {
    fn from_baml_value(
        value: baml_client_rust::types::BamlValue,
    ) -> baml_client_rust::BamlResult<Self> {
        match value {
            baml_client_rust::types::BamlValue::Class(_class_name, map) => {
                let priority = map
                    .get("priority")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'priority' in IntegerLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                let httpStatus = map
                    .get("httpStatus")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'httpStatus' in IntegerLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                let maxRetries = map
                    .get("maxRetries")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'maxRetries' in IntegerLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                Ok(Self::new(priority, httpStatus, maxRetries))
            }
            _ => Err(baml_client_rust::BamlError::deserialization(format!(
                "Expected class, got {:?}",
                value
            ))),
        }
    }
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct MixedLiterals {
    pub id: String,

    pub r#type: String,

    pub level: String,

    pub isActive: String,

    pub apiVersion: String,
}

impl MixedLiterals {
    /// Create a new MixedLiterals instance
    pub fn new(
        id: String,
        r#type: String,
        level: String,
        isActive: String,
        apiVersion: String,
    ) -> Self {
        Self {
            id,
            r#type,
            level,
            isActive,
            apiVersion,
        }
    }
}

impl Default for MixedLiterals {
    fn default() -> Self {
        Self::new(
            String::new(),
            String::new(),
            String::new(),
            String::new(),
            String::new(),
        )
    }
}

// BAML trait implementations
impl baml_client_rust::types::ToBamlValue for MixedLiterals {
    fn to_baml_value(self) -> baml_client_rust::BamlResult<baml_client_rust::types::BamlValue> {
        let mut map = baml_client_rust::types::BamlMap::new();
        map.insert("id".to_string(), self.id.to_baml_value()?);
        map.insert("r#type".to_string(), self.r#type.to_baml_value()?);
        map.insert("level".to_string(), self.level.to_baml_value()?);
        map.insert("isActive".to_string(), self.isActive.to_baml_value()?);
        map.insert("apiVersion".to_string(), self.apiVersion.to_baml_value()?);
        Ok(baml_client_rust::types::BamlValue::Class(
            "MixedLiterals".to_string(),
            map,
        ))
    }
}

impl baml_client_rust::types::FromBamlValue for MixedLiterals {
    fn from_baml_value(
        value: baml_client_rust::types::BamlValue,
    ) -> baml_client_rust::BamlResult<Self> {
        match value {
            baml_client_rust::types::BamlValue::Class(_class_name, map) => {
                let id = map
                    .get("id")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'id' in MixedLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                let r#type = map
                    .get("r#type")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'r#type' in MixedLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                let level = map
                    .get("level")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'level' in MixedLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                let isActive = map
                    .get("isActive")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'isActive' in MixedLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                let apiVersion = map
                    .get("apiVersion")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'apiVersion' in MixedLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                Ok(Self::new(id, r#type, level, isActive, apiVersion))
            }
            _ => Err(baml_client_rust::BamlError::deserialization(format!(
                "Expected class, got {:?}",
                value
            ))),
        }
    }
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct StringLiterals {
    pub status: String,

    pub environment: String,

    pub method: String,
}

impl StringLiterals {
    /// Create a new StringLiterals instance
    pub fn new(status: String, environment: String, method: String) -> Self {
        Self {
            status,
            environment,
            method,
        }
    }
}

impl Default for StringLiterals {
    fn default() -> Self {
        Self::new(String::new(), String::new(), String::new())
    }
}

// BAML trait implementations
impl baml_client_rust::types::ToBamlValue for StringLiterals {
    fn to_baml_value(self) -> baml_client_rust::BamlResult<baml_client_rust::types::BamlValue> {
        let mut map = baml_client_rust::types::BamlMap::new();
        map.insert("status".to_string(), self.status.to_baml_value()?);
        map.insert("environment".to_string(), self.environment.to_baml_value()?);
        map.insert("method".to_string(), self.method.to_baml_value()?);
        Ok(baml_client_rust::types::BamlValue::Class(
            "StringLiterals".to_string(),
            map,
        ))
    }
}

impl baml_client_rust::types::FromBamlValue for StringLiterals {
    fn from_baml_value(
        value: baml_client_rust::types::BamlValue,
    ) -> baml_client_rust::BamlResult<Self> {
        match value {
            baml_client_rust::types::BamlValue::Class(_class_name, map) => {
                let status = map
                    .get("status")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'status' in StringLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                let environment = map
                    .get("environment")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'environment' in StringLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                let method = map
                    .get("method")
                    .ok_or_else(|| {
                        baml_client_rust::BamlError::deserialization(format!(
                            "Missing field 'method' in StringLiterals"
                        ))
                    })
                    .and_then(|v| {
                        baml_client_rust::types::FromBamlValue::from_baml_value(v.clone())
                    })?;
                Ok(Self::new(status, environment, method))
            }
            _ => Err(baml_client_rust::BamlError::deserialization(format!(
                "Expected class, got {:?}",
                value
            ))),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Union2BoolKFalseOrBoolKTrue {
    Bool(bool),
    Bool(bool),
}

impl Union2BoolKFalseOrBoolKTrue {
    /// Check if this union is a Bool variant
    pub fn is_bool(&self) -> bool {
        matches!(self, Self::Bool(_))
    }
    /// Get the Bool value if this union contains it
    pub fn as_bool(&self) -> Option<&bool> {
        match self {
            Self::Bool(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Bool value, consuming the union
    pub fn into_bool(self) -> Option<bool> {
        match self {
            Self::Bool(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Bool value if this union contains it
    pub fn as_bool_mut(&mut self) -> Option<&mut bool> {
        match self {
            Self::Bool(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union2BoolKFalseOrBoolKTrue with a Bool variant
    pub fn bool(value: bool) -> Self {
        Self::Bool(value)
    }

    /// Check if this union is a Bool variant
    pub fn is_bool(&self) -> bool {
        matches!(self, Self::Bool(_))
    }
    /// Get the Bool value if this union contains it
    pub fn as_bool(&self) -> Option<&bool> {
        match self {
            Self::Bool(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Bool value, consuming the union
    pub fn into_bool(self) -> Option<bool> {
        match self {
            Self::Bool(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Bool value if this union contains it
    pub fn as_bool_mut(&mut self) -> Option<&mut bool> {
        match self {
            Self::Bool(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union2BoolKFalseOrBoolKTrue with a Bool variant
    pub fn bool(value: bool) -> Self {
        Self::Bool(value)
    }
}

/// Pattern matching helper for Union2BoolKFalseOrBoolKTrue
impl Union2BoolKFalseOrBoolKTrue {
    /// Match on the union variant and apply the corresponding function
    pub fn match_variant<T>(
        &self,
        bool: impl FnOnce(&bool) -> T,
        bool: impl FnOnce(&bool) -> T,
    ) -> T {
        match self {
            Self::Bool(v) => bool(v),
            Self::Bool(v) => bool(v),
        }
    }

    /// Match on the union variant and apply the corresponding function, consuming the union
    pub fn match_variant_owned<T>(
        self,
        bool: impl FnOnce(bool) -> T,
        bool: impl FnOnce(bool) -> T,
    ) -> T {
        match self {
            Self::Bool(v) => bool(v),
            Self::Bool(v) => bool(v),
        }
    }
}

/// Display implementation that shows the variant name and value
impl std::fmt::Display for Union2BoolKFalseOrBoolKTrue {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Bool(v) => write!(f, "Bool({:?})", v),
            Self::Bool(v) => write!(f, "Bool({:?})", v),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Union3IntK1OrIntK2OrIntK3 {
    Int(i64),
    Int(i64),
    Int(i64),
}

impl Union3IntK1OrIntK2OrIntK3 {
    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3IntK1OrIntK2OrIntK3 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3IntK1OrIntK2OrIntK3 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3IntK1OrIntK2OrIntK3 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }
}

/// Pattern matching helper for Union3IntK1OrIntK2OrIntK3
impl Union3IntK1OrIntK2OrIntK3 {
    /// Match on the union variant and apply the corresponding function
    pub fn match_variant<T>(
        &self,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
    ) -> T {
        match self {
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
        }
    }

    /// Match on the union variant and apply the corresponding function, consuming the union
    pub fn match_variant_owned<T>(
        self,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
    ) -> T {
        match self {
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
        }
    }
}

/// Display implementation that shows the variant name and value
impl std::fmt::Display for Union3IntK1OrIntK2OrIntK3 {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Union3IntK200OrIntK404OrIntK500 {
    Int(i64),
    Int(i64),
    Int(i64),
}

impl Union3IntK200OrIntK404OrIntK500 {
    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3IntK200OrIntK404OrIntK500 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3IntK200OrIntK404OrIntK500 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3IntK200OrIntK404OrIntK500 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }
}

/// Pattern matching helper for Union3IntK200OrIntK404OrIntK500
impl Union3IntK200OrIntK404OrIntK500 {
    /// Match on the union variant and apply the corresponding function
    pub fn match_variant<T>(
        &self,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
    ) -> T {
        match self {
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
        }
    }

    /// Match on the union variant and apply the corresponding function, consuming the union
    pub fn match_variant_owned<T>(
        self,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
    ) -> T {
        match self {
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
        }
    }
}

/// Display implementation that shows the variant name and value
impl std::fmt::Display for Union3IntK200OrIntK404OrIntK500 {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Union3KactiveOrKinactiveOrKpending {
    String(String),
    String(String),
    String(String),
}

impl Union3KactiveOrKinactiveOrKpending {
    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3KactiveOrKinactiveOrKpending with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3KactiveOrKinactiveOrKpending with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3KactiveOrKinactiveOrKpending with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }
}

/// Pattern matching helper for Union3KactiveOrKinactiveOrKpending
impl Union3KactiveOrKinactiveOrKpending {
    /// Match on the union variant and apply the corresponding function
    pub fn match_variant<T>(
        &self,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
    ) -> T {
        match self {
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
        }
    }

    /// Match on the union variant and apply the corresponding function, consuming the union
    pub fn match_variant_owned<T>(
        self,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
    ) -> T {
        match self {
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
        }
    }
}

/// Display implementation that shows the variant name and value
impl std::fmt::Display for Union3KactiveOrKinactiveOrKpending {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Union3KadminOrKguestOrKuser {
    String(String),
    String(String),
    String(String),
}

impl Union3KadminOrKguestOrKuser {
    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3KadminOrKguestOrKuser with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3KadminOrKguestOrKuser with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3KadminOrKguestOrKuser with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }
}

/// Pattern matching helper for Union3KadminOrKguestOrKuser
impl Union3KadminOrKguestOrKuser {
    /// Match on the union variant and apply the corresponding function
    pub fn match_variant<T>(
        &self,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
    ) -> T {
        match self {
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
        }
    }

    /// Match on the union variant and apply the corresponding function, consuming the union
    pub fn match_variant_owned<T>(
        self,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
    ) -> T {
        match self {
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
        }
    }
}

/// Display implementation that shows the variant name and value
impl std::fmt::Display for Union3KadminOrKguestOrKuser {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Union3KdevOrKprodOrKstaging {
    String(String),
    String(String),
    String(String),
}

impl Union3KdevOrKprodOrKstaging {
    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3KdevOrKprodOrKstaging with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3KdevOrKprodOrKstaging with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3KdevOrKprodOrKstaging with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }
}

/// Pattern matching helper for Union3KdevOrKprodOrKstaging
impl Union3KdevOrKprodOrKstaging {
    /// Match on the union variant and apply the corresponding function
    pub fn match_variant<T>(
        &self,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
    ) -> T {
        match self {
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
        }
    }

    /// Match on the union variant and apply the corresponding function, consuming the union
    pub fn match_variant_owned<T>(
        self,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
    ) -> T {
        match self {
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
        }
    }
}

/// Display implementation that shows the variant name and value
impl std::fmt::Display for Union3KdevOrKprodOrKstaging {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Union3KerrorOrKsuccessOrKtimeout {
    String(String),
    String(String),
    String(String),
}

impl Union3KerrorOrKsuccessOrKtimeout {
    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3KerrorOrKsuccessOrKtimeout with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3KerrorOrKsuccessOrKtimeout with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3KerrorOrKsuccessOrKtimeout with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }
}

/// Pattern matching helper for Union3KerrorOrKsuccessOrKtimeout
impl Union3KerrorOrKsuccessOrKtimeout {
    /// Match on the union variant and apply the corresponding function
    pub fn match_variant<T>(
        &self,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
    ) -> T {
        match self {
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
        }
    }

    /// Match on the union variant and apply the corresponding function, consuming the union
    pub fn match_variant_owned<T>(
        self,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
    ) -> T {
        match self {
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
        }
    }
}

/// Display implementation that shows the variant name and value
impl std::fmt::Display for Union3KerrorOrKsuccessOrKtimeout {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Union3Kv1OrKv2OrKv3 {
    String(String),
    String(String),
    String(String),
}

impl Union3Kv1OrKv2OrKv3 {
    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3Kv1OrKv2OrKv3 with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3Kv1OrKv2OrKv3 with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union3Kv1OrKv2OrKv3 with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }
}

/// Pattern matching helper for Union3Kv1OrKv2OrKv3
impl Union3Kv1OrKv2OrKv3 {
    /// Match on the union variant and apply the corresponding function
    pub fn match_variant<T>(
        &self,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
    ) -> T {
        match self {
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
        }
    }

    /// Match on the union variant and apply the corresponding function, consuming the union
    pub fn match_variant_owned<T>(
        self,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
    ) -> T {
        match self {
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
        }
    }
}

/// Display implementation that shows the variant name and value
impl std::fmt::Display for Union3Kv1OrKv2OrKv3 {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Union4IntK0OrIntK1OrIntK3OrIntK5 {
    Int(i64),
    Int(i64),
    Int(i64),
    Int(i64),
}

impl Union4IntK0OrIntK1OrIntK3OrIntK5 {
    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union4IntK0OrIntK1OrIntK3OrIntK5 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union4IntK0OrIntK1OrIntK3OrIntK5 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union4IntK0OrIntK1OrIntK3OrIntK5 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union4IntK0OrIntK1OrIntK3OrIntK5 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }
}

/// Pattern matching helper for Union4IntK0OrIntK1OrIntK3OrIntK5
impl Union4IntK0OrIntK1OrIntK3OrIntK5 {
    /// Match on the union variant and apply the corresponding function
    pub fn match_variant<T>(
        &self,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
    ) -> T {
        match self {
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
        }
    }

    /// Match on the union variant and apply the corresponding function, consuming the union
    pub fn match_variant_owned<T>(
        self,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
    ) -> T {
        match self {
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
        }
    }
}

/// Display implementation that shows the variant name and value
impl std::fmt::Display for Union4IntK0OrIntK1OrIntK3OrIntK5 {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Union4KDELETEOrKGETOrKPOSTOrKPUT {
    String(String),
    String(String),
    String(String),
    String(String),
}

impl Union4KDELETEOrKGETOrKPOSTOrKPUT {
    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union4KDELETEOrKGETOrKPOSTOrKPUT with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union4KDELETEOrKGETOrKPOSTOrKPUT with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union4KDELETEOrKGETOrKPOSTOrKPUT with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union4KDELETEOrKGETOrKPOSTOrKPUT with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }
}

/// Pattern matching helper for Union4KDELETEOrKGETOrKPOSTOrKPUT
impl Union4KDELETEOrKGETOrKPOSTOrKPUT {
    /// Match on the union variant and apply the corresponding function
    pub fn match_variant<T>(
        &self,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
    ) -> T {
        match self {
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
        }
    }

    /// Match on the union variant and apply the corresponding function, consuming the union
    pub fn match_variant_owned<T>(
        self,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
    ) -> T {
        match self {
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
        }
    }
}

/// Display implementation that shows the variant name and value
impl std::fmt::Display for Union4KDELETEOrKGETOrKPOSTOrKPUT {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Union4KarchivedOrKdeletedOrKdraftOrKpublished {
    String(String),
    String(String),
    String(String),
    String(String),
}

impl Union4KarchivedOrKdeletedOrKdraftOrKpublished {
    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union4KarchivedOrKdeletedOrKdraftOrKpublished with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union4KarchivedOrKdeletedOrKdraftOrKpublished with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union4KarchivedOrKdeletedOrKdraftOrKpublished with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }

    /// Check if this union is a String variant
    pub fn is_string(&self) -> bool {
        matches!(self, Self::String(_))
    }
    /// Get the String value if this union contains it
    pub fn as_string(&self) -> Option<&String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the String value, consuming the union
    pub fn into_string(self) -> Option<String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the String value if this union contains it
    pub fn as_string_mut(&mut self) -> Option<&mut String> {
        match self {
            Self::String(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union4KarchivedOrKdeletedOrKdraftOrKpublished with a String variant
    pub fn string(value: String) -> Self {
        Self::String(value)
    }
}

/// Pattern matching helper for Union4KarchivedOrKdeletedOrKdraftOrKpublished
impl Union4KarchivedOrKdeletedOrKdraftOrKpublished {
    /// Match on the union variant and apply the corresponding function
    pub fn match_variant<T>(
        &self,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
        string: impl FnOnce(&String) -> T,
    ) -> T {
        match self {
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
        }
    }

    /// Match on the union variant and apply the corresponding function, consuming the union
    pub fn match_variant_owned<T>(
        self,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
        string: impl FnOnce(String) -> T,
    ) -> T {
        match self {
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
            Self::String(v) => string(v),
        }
    }
}

/// Display implementation that shows the variant name and value
impl std::fmt::Display for Union4KarchivedOrKdeletedOrKdraftOrKpublished {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
            Self::String(v) => write!(f, "String({:?})", v),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Union5IntK1OrIntK2OrIntK3OrIntK4OrIntK5 {
    Int(i64),
    Int(i64),
    Int(i64),
    Int(i64),
    Int(i64),
}

impl Union5IntK1OrIntK2OrIntK3OrIntK4OrIntK5 {
    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union5IntK1OrIntK2OrIntK3OrIntK4OrIntK5 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union5IntK1OrIntK2OrIntK3OrIntK4OrIntK5 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union5IntK1OrIntK2OrIntK3OrIntK4OrIntK5 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union5IntK1OrIntK2OrIntK3OrIntK4OrIntK5 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union5IntK1OrIntK2OrIntK3OrIntK4OrIntK5 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }
}

/// Pattern matching helper for Union5IntK1OrIntK2OrIntK3OrIntK4OrIntK5
impl Union5IntK1OrIntK2OrIntK3OrIntK4OrIntK5 {
    /// Match on the union variant and apply the corresponding function
    pub fn match_variant<T>(
        &self,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
    ) -> T {
        match self {
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
        }
    }

    /// Match on the union variant and apply the corresponding function, consuming the union
    pub fn match_variant_owned<T>(
        self,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
    ) -> T {
        match self {
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
        }
    }
}

/// Display implementation that shows the variant name and value
impl std::fmt::Display for Union5IntK1OrIntK2OrIntK3OrIntK4OrIntK5 {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Union5IntK200OrIntK201OrIntK400OrIntK404OrIntK500 {
    Int(i64),
    Int(i64),
    Int(i64),
    Int(i64),
    Int(i64),
}

impl Union5IntK200OrIntK201OrIntK400OrIntK404OrIntK500 {
    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union5IntK200OrIntK201OrIntK400OrIntK404OrIntK500 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union5IntK200OrIntK201OrIntK400OrIntK404OrIntK500 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union5IntK200OrIntK201OrIntK400OrIntK404OrIntK500 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union5IntK200OrIntK201OrIntK400OrIntK404OrIntK500 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union5IntK200OrIntK201OrIntK400OrIntK404OrIntK500 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }
}

/// Pattern matching helper for Union5IntK200OrIntK201OrIntK400OrIntK404OrIntK500
impl Union5IntK200OrIntK201OrIntK400OrIntK404OrIntK500 {
    /// Match on the union variant and apply the corresponding function
    pub fn match_variant<T>(
        &self,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
    ) -> T {
        match self {
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
        }
    }

    /// Match on the union variant and apply the corresponding function, consuming the union
    pub fn match_variant_owned<T>(
        self,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
    ) -> T {
        match self {
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
        }
    }
}

/// Display implementation that shows the variant name and value
impl std::fmt::Display for Union5IntK200OrIntK201OrIntK400OrIntK404OrIntK500 {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(untagged)]
pub enum Union7IntK0OrIntK1OrIntK13OrIntK2OrIntK3OrIntK5OrIntK8 {
    Int(i64),
    Int(i64),
    Int(i64),
    Int(i64),
    Int(i64),
    Int(i64),
    Int(i64),
}

impl Union7IntK0OrIntK1OrIntK13OrIntK2OrIntK3OrIntK5OrIntK8 {
    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union7IntK0OrIntK1OrIntK13OrIntK2OrIntK3OrIntK5OrIntK8 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union7IntK0OrIntK1OrIntK13OrIntK2OrIntK3OrIntK5OrIntK8 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union7IntK0OrIntK1OrIntK13OrIntK2OrIntK3OrIntK5OrIntK8 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union7IntK0OrIntK1OrIntK13OrIntK2OrIntK3OrIntK5OrIntK8 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union7IntK0OrIntK1OrIntK13OrIntK2OrIntK3OrIntK5OrIntK8 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union7IntK0OrIntK1OrIntK13OrIntK2OrIntK3OrIntK5OrIntK8 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }

    /// Check if this union is a Int variant
    pub fn is_int(&self) -> bool {
        matches!(self, Self::Int(_))
    }
    /// Get the Int value if this union contains it
    pub fn as_int(&self) -> Option<&i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Extract the Int value, consuming the union
    pub fn into_int(self) -> Option<i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Get a mutable reference to the Int value if this union contains it
    pub fn as_int_mut(&mut self) -> Option<&mut i64> {
        match self {
            Self::Int(v) => Some(v),
            _ => None,
        }
    }

    /// Create a new Union7IntK0OrIntK1OrIntK13OrIntK2OrIntK3OrIntK5OrIntK8 with a Int variant
    pub fn int(value: i64) -> Self {
        Self::Int(value)
    }
}

/// Pattern matching helper for Union7IntK0OrIntK1OrIntK13OrIntK2OrIntK3OrIntK5OrIntK8
impl Union7IntK0OrIntK1OrIntK13OrIntK2OrIntK3OrIntK5OrIntK8 {
    /// Match on the union variant and apply the corresponding function
    pub fn match_variant<T>(
        &self,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
        int: impl FnOnce(&i64) -> T,
    ) -> T {
        match self {
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
        }
    }

    /// Match on the union variant and apply the corresponding function, consuming the union
    pub fn match_variant_owned<T>(
        self,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
        int: impl FnOnce(i64) -> T,
    ) -> T {
        match self {
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
            Self::Int(v) => int(v),
        }
    }
}

/// Display implementation that shows the variant name and value
impl std::fmt::Display for Union7IntK0OrIntK1OrIntK13OrIntK2OrIntK3OrIntK5OrIntK8 {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
            Self::Int(v) => write!(f, "Int({:?})", v),
        }
    }
}
