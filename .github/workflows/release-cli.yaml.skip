name: Release BAML CLI

on:
  release:
    types: [published, unpublished, created, edited, prereleased]

permissions:
  contents: read

jobs:
  build-cli:
    uses: ./.github/workflows/release/cli.reusable.yaml