name: "Setup Development Tools"
description: "Setup common development tools"
runs:
  using: "composite"
  steps:
    - name: Setup mise
      uses: jdx/mise-action@v3
      with:
        cache: true
