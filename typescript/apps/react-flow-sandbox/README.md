# React Flow Sandbox

A minimal Next.js sandbox for experimenting with data structures that map Mermaid diagrams into React Flow.

## Getting started

```bash
pnpm install
pnpm --filter react-flow-sandbox dev
```

Then open http://localhost:3000.
